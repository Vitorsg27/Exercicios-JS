const calculos = (n1 = 1,n2 = 1) => {
    soma = n1 + n2
    subtracao = n1 - n2
    multiplicacao = n1 * n2
    divisao = n1 / n2
    console.log(`${n1} + ${n2} = ${soma} \n${n1} - ${n2} = ${subtracao} \n${n1} x ${n2} = ${multiplicacao} \n${n1} / ${n2} = ${divisao} \n`);
}

calculos(10,20);